import styled from 'styled-components';

const SearchButton = styled.button`
	background: transparent;
	border: none;
	margin-left: -33px;
	position: absolute;
	top: 0.35rem;
	bottom: 0.2rem;
	right: 0.9rem;
`;

export { SearchButton };
