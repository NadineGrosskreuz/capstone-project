import styled from 'styled-components';

const IconButton = styled.button`
	background-color: transparent;
	border: none;
	cursor: pointer;
	position: absolute;
	top: 1rem;
	right: 1.5rem;
`;

export { IconButton };
