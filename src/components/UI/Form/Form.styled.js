import styled from 'styled-components';

const FormStyled = styled.form`
	background-color: var(--orange);
	font-size: 1.6rem;
	border-radius: 20px;
	padding: 15px;
	width: 95vw;
`;

export { FormStyled };
